logins db as follows: 
    username VARCHAR(50) PRIMARY KEY,
    password VARCHAR(255),
    permission VARCHAR(50)


if you need login perms, make your account on the aws db, username is your email for google auth
purposes
